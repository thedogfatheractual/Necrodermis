# ── Necrodermis — user keybind overrides ──────────────────────────────────────
# Add personal keybinds here — loaded after conf/keybinds.conf so they win.
# $mainMod is already defined in conf/keybinds.conf
